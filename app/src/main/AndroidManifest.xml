<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.ctakesoft.demo"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DemoListActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".SimpleCustomTabActivity"
            android:label="@string/title_activity_simple_chrome_tab"
            android:parentActivityName=".demo.DemoListActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".DemoListActivity" />
        </activity>
        <activity
            android:name=".CustomUIActivity"
            android:label="@string/title_activity_customized_chrome_tab"
            android:parentActivityName=".DemoListActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".DemoListActivity" />
        </activity>

        <activity android:name=".NextActivity">
        </activity>

        <!-- TODO: WebViewActivityのタイトルなど -->
        <activity
            android:name="com.ctakesoft.ctassistant.AssistantWebViewActivity"
            android:label="@string/title_activity_webview"
            android:parentActivityName=".DemoListActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".DemoListActivity" />
        </activity>
        <receiver android:name="com.ctakesoft.ctassistant.ShareBroadcastReceiver"/>
    </application>

</manifest>
